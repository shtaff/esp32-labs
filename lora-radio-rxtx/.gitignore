.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Local secrets: WiFi credentials and the transmitter site coordinate.
# See secrets.ini.example.
secrets.ini

# Retrieved logs. These carry a GPS track of wherever the receiver was taken,
# which is personal location data - keep it out of the repository.
*.csv
